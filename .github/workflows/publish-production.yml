name: Legacy Publish Workflow Placeholder

on:
  workflow_dispatch:

jobs:
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow placeholder
        run: echo "Use .github/workflows/deploy.yml instead."